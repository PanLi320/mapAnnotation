import React, { PropTypes } from 'react';


const MapMarker = React.createClass({
    render: function () {
        return(
            <div/>
        );
    }
});

exports.mapCenter = function (center) {
    console.log(center);
};

export default MapMarker;