import React, { Component } from 'react';
import MapRegion from './MapRegion';

const Map = React.createClass({
    render: function () {
        return (
            <MapRegion/>
        );
    }
});

export default Map;